{
  "$id": "reservation-update",
  "title": "Update Reservation parameters",
  "type": "object",
  "properties": {
    "chargingStationId": {
      "$ref": "chargingstation#/definitions/id"
    },
    "connectorId": {
      "$ref": "chargingstation#/definitions/connector/properties/connectorId"
    },
    "fromDate": {
      "type": "string",
      "format": "date-time",
      "customType": "date",
      "sanitize": "mongo"
    },
    "toDate": {
      "type": "string",
      "format": "date-time",
      "customType": "date",
      "sanitize": "mongo"
    },
    "expiryDate": {
      "type": "string",
      "format": "date-time",
      "customType": "date",
      "sanitize": "mongo"
    },
    "idTag": {
      "$ref": "tag#/definitions/id"
    },
    "parentIdTag": {
      "$ref": "tag#/definitions/id"
    },
    "reservationId": {
      "type": "number",
      "sanitize": "mongo"
    }
  },
  "required": [
    "chargingStationID",
    "reservationId"
  ]
}